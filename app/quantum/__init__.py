"""Provider-agnostic quantum-classical simulation core.

This package contains the algorithms (VQE on H2 and LiH), the abstractions
that let them target either a CPU statevector simulator or a GPU statevector
simulator without touching Akamai-specific code, and reference data used to
validate convergence.
"""
